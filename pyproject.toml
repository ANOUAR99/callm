[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Function calling engine using constrained decoding"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
    "numpy>=1.26.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"